# Status / TODO

See `README.md` for what this project is, `ARCHITECTURE.md` for design decisions, `AGENTS.md` for always-loaded agent behavior rules. Update this file after functions are added to capture current progress in the repo.

Done:
- [x] Scaffold project, pom.xml
- [x] ClaudeCodeClient: real subprocess wrapper around headless `claude -p`, unit-tested and empirically verified under cron-like conditions (see ARCHITECTURE.md decision #1)
- [x] NotionModels + NotionExtractor: records + small reusable per-property-shape deserializers (title/rich_text, status/select, number/rollup/formula, relation, date, created_time/last_edited_time), tested against the three fixtures. `fetchX` empirically verified live (see below). Along the way, found and removed 3 undocumented, unused Tasks properties (Place, a bare Date, Total Mins Copy) from both the real capture and the sanitized fixture — since deleted from Notion itself, so the fixture now matches the live schema exactly again.
- [x] SnapshotBuilder: joins tasks to their parent project(s) via each task's own Parent relation (not the project's Child Tasks - not assumed authoritative), tolerates dangling relation ids (mirrors a real one found in the data), resolves "latest session note per task" by max-date over that task's Minutes entries. No filtering/ranking - every project/task passed in appears in the output; that judgment is Claude's job at PromptBuilder, not this layer's.
- [x] DigestLog: minimal on purpose - one entry, not an accumulating history (per ARCHITECTURE.md decision #3). `LogEntry(String date, String recommendation)` stores the LLM's raw text as-is; `date` is a plain ISO string, not `LocalDate`, to avoid a jackson-datatype-jsr310 dependency for one flat field. No atomic write, no directory auto-creation, no retry - deliberately skipped for now to prioritize reaching a working demo over hardening.

Path to the working demo (complete):
- [x] PromptBuilder: plain-text prompt (framing + yesterday's recommendation if any + per-project/per-task rendering + closing ask), null-safe so unset fields are omitted rather than printed as "null". First-draft wording - to be tuned once real output from an actual run is visible, not polished blind.
- [x] DiscordNotifier: webhook POST via java.net.http.HttpClient, JSON body built with Jackson (not string concat), truncates to Discord's 2000-char content limit. Empirically verified live (see below).
- [x] Main.java: wired NotionExtractor -> SnapshotBuilder -> PromptBuilder/ClaudeCodeClient -> DiscordNotifier -> DigestLog. Has a `--dry-run` flag (parses the checked-in sanitized fixtures instead of NotionExtractor.fetchX, prints to stdout instead of DiscordNotifier.send) used to verify the pipeline safely before the live run - see below for how `--dry-run` was validated.
- [x] First real end-to-end run against live Notion + live Discord (you asked for it explicitly). No errors - real Notion fetch, real SnapshotBuilder join, real Claude ranking (correctly identified this very project as the highest-priority task, since other work is blocked on it), real Discord post, real digest-log.json write. This is the first genuinely working version of the whole tool, not just individually-tested pieces.

MVP pipeline complete - everything below is hardening/polish, not required for the tool to work:
- [x] PromptBuilder closing ask changed from "single highest-value pick" to a ranked list of 3-5 items with reasoning each - the single-pick version didn't satisfy: forced picking for the user rather than letting them choose. Verified live: correctly ranked 4 real options, explained exclusions, and picked up on yesterday's two recommended tasks now being marked Done in real Notion data.
- [ ] Deploy: real cron job on the homelab machine. CLAUDE_EXECUTABLE_PATH needs its homelab value confirmed there (see ARCHITECTURE.md decision #1 - dev and homelab have `claude` installed in different places); one more live verification run from actual cron (no TTY, real minimal env) would close the loop on decision #1's original concern for real, not just simulated.
- [x] Detect stale/expired Claude Code credentials and notify instead of failing silently. `ClaudeCodeClient.isLoggedIn()` calls the real `claude auth status --json` (confirmed empirically: missing and corrupted credentials both collapse to the same `loggedIn:false` shape, so no need to distinguish those cases) - checked first thing in Main, before any Notion calls, so a broken login fails fast instead of burning API calls on a run that can't finish. On failure: alert via the same `--dry-run`-aware delivery path as the normal digest (so dry-run never touches the real webhook, alert or not), skip DigestLog.write entirely (an auth failure isn't a recommendation - writing one would corrupt tomorrow's continuity prompt). Empirically verified all three states via the manual harness: real logged-in (`true`), and a deliberately broken isolated fake-HOME environment run through the real subprocess path end-to-end (`false`, not just a canned-string unit test) - never touched real credentials either way.
- Skipped by choice: guarding NotionExtractor.fetchAllPages against a has_more=true/next_cursor=null infinite loop. Low-probability edge case against a well-behaved documented API; not worth the code for now.
- [ ] Revisit whether TaskSnapshot needs created/edited timestamps once PromptBuilder's actual "how stale is this task" needs are known - not needed yet, cheap to add later

Found by an independent code-review agent (2026-09-25):
- [x] ClaudeCodeClient.launch()'s 120s timeout didn't actually bound anything: readAll() blocked reading the subprocess's full output *before* waitFor(TIMEOUT_SECONDS,...) was ever reached. Fixed by redirecting output straight to a temp file (ProcessBuilder#redirectOutput) instead of reading it from a pipe - a piped read can block until the process writes/closes, which is exactly what defeated the timeout; a file has no such bounded buffer to block on either side, so waitFor(timeout) now genuinely bounds the wait, and the output is read from disk afterward (fast, since the process already finished). Empirically verified with a fake executable substituted for CLAUDE_EXECUTABLE_PATH: one that hangs forever confirms the fix actually times out at ~120s and throws (not tested against the old broken code - the bug's mechanism was clear enough from reading, this verifies the fix, not the regression), and one that exits normally confirms output/exit code still come through correctly via the file-redirect path.
- [x] NotionExtractor's and DiscordNotifier's HttpClient instances have no connect/request timeout configured - fixed with a 30s `HttpClient.connectTimeout` (bounds the TCP handshake) plus a 30s `HttpRequest.timeout` (bounds the whole wait for a response - the one that actually matters for "server accepts the connection but never responds"). `HttpTimeoutException extends IOException`, so no new exception handling was needed anywhere - it flows through the same `catch (IOException | InterruptedException e)` Main already had. Empirically verified: a throwaway `ServerSocket` that accepts a connection and then never responds caused the request to correctly throw `HttpTimeoutException` at exactly 30s instead of hanging.
- [ ] Main's auth-failure branch calls deliver(dryRun, dotenv, alert) before printing the alert to stderr - if deliver() itself throws (e.g. Discord unreachable), the real cause (stale Claude Code login) never gets printed, only a generic top-level "Digest run failed" message.
- [ ] Main's DigestLog.write() runs after the Discord post - if the write fails (disk full, permissions), the whole run reports as failed even though the digest was already delivered; a naive retry would re-post the same digest.
- [ ] NotionExtractor.fetchAllPages (the pagination/cursor-chaining loop) has zero test coverage, despite a package-private constructor (NotionExtractor(String, HttpClient)) existing specifically to allow injecting a fake HttpClient for exactly this.
- [ ] Main.run()'s orchestration is entirely untested, including the specifically-documented "skip DigestLog.write on auth failure" behavior (ARCHITECTURE.md decision #3 / the isLoggedIn item above) - nothing would catch a regression there.
- Already known and deliberately deprioritized, independently reconfirmed by the same review: the raw string-concatenated JSON body in fetchAllPages's pagination request. Still low severity; noting the reconfirmation, not re-opening it as new.
