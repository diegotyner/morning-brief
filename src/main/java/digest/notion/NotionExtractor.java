package digest.notion;

/**
 * Fetches raw data from Notion (Long Term, Tasks, Minutes databases) via the Notion API.
 * TODO: implement extraction logic. Must match the exact JSON shape captured in
 * src/test/resources/notion-sample-*.json — never guess the shape.
 */
public class NotionExtractor {
}
